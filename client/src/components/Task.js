/**
 * Task section
 * each task section may have a dropdown with subtasks
 * there is also a chechbox, each can be dragged and dropped to any other section
 * and they can be deleted
 */

import React from 'react'

export default function Task() {
    return (
        <div>
            
        </div>
    )
}
