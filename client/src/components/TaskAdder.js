/**
 * TaskAdder component
 * on the click of a button at the top right of the screen,
 * this window appears
 * there is a text input, a start and end date, and repeat, and it should be smart
 * and sort things out based on the start and end date, and
 * do text parsing to sort out tasks and subtasks
 */

import React from 'react'

export default function TaskAdder() {
    return (
        <div>
            
        </div>
    )
}
