/**
 * Subtask component
 * Can be checked off or deleted
 * cannot be dragged between tasks
 */

import React from 'react'

export default function Subtask() {
    return (
        <div>
            
        </div>
    )
}
